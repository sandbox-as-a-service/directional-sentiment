meta {
  name: health
  type: http
  seq: 1
}

get {
  url: {{URL}}/api/health
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.status: eq ok
}

settings {
  encodeUrl: true
}
